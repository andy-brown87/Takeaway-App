{% extends 'base.jinja' %}

{% block content %}

<ul id = "customers">
<style>
ul#menu li {
  display:inline;
}
</style>
    
        <h1>Customers:</h1>
            
            <ul id="menu">
            {% for customer in customers %}
                <th>Name:</th>
                <li>{{customer.name}}</li>
                <th>Phone Number:</th>
                <li>{{customer.phone_number}}</li>
                <th>Address:</th>
                <li>{{customer.address}}</li>
                <br>
            {% endfor %}
            </ul>
            </span>


{% endblock %}


{# <h1>Customers</h1>

<ul id = "customer list">

<div>
    <b>{{ customers.name }}</b>
        <span>
        <h1>Please enter a new customer</h1>
        <form style="display:inline" action="/customer/{{customers.id}}" method="POST">

            <label for="name">Name:</label>
            <input type="text" name="name" id="name" />

            <label for="phone number">Phone Number:</label>
            <input type="number" name="phone number" id="phone number" />

            <label for="address">Address:</label>
            <input type = "text" name="address" id="address" />

            <input type="submit" value="Enter Customer">

        </form> #}

        {# <h3>See list of all Customers:</h3>
            <a href="/customers/all_customers">Customers</a>
            </span>
</div> #}

