<h1>Items</h1>

{% for item in item %}

<ul id = "items">
    
    <li>
        <a href="/item/{{item.index(items)}}">{{items.name}}</a>
    </li>

    <li>
        <a href="/item/{{item.index(item)}}">{{item.price}}</a>
    </li>

{% endfor %}

{% endblock %}
</ul>
